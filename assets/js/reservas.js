document.querySelectorAll('[data-submit-form]').forEach((form)=>{form.addEventListener('submit',(event)=>{if(!form.checkValidity()){event.preventDefault();form.reportValidity();return}const button=form.querySelector('button[type="submit"]');if(button){button.disabled=true;button.classList.add('is-loading')}})});document.querySelectorAll('[data-copy-pix]').forEach((button)=>{button.addEventListener('click',async()=>{const card=button.closest('.payment-card');const code=card.querySelector('[data-pix-code]').value;const feedback=card.querySelector('.copy-feedback');try{await navigator.clipboard.writeText(code);feedback.textContent='Codigo Pix copiado.'}catch{card.querySelector('[data-pix-code]').select();document.execCommand('copy');feedback.textContent='Codigo Pix copiado.'}})});document.querySelectorAll('input[name="checkin"]').forEach((input)=>{input.addEventListener('change',()=>{const checkout=document.querySelector('input[name="checkout"]');if(checkout){const date=new Date(input.value+'T12:00:00');date.setDate(date.getDate()+1);checkout.min=date.toISOString().slice(0,10);if(checkout.value&&checkout.value<=input.value)checkout.value=''}})});document.querySelectorAll('[data-upload-form]').forEach((form)=>{form.addEventListener('submit',()=>{const progress=form.querySelector('progress');if(progress){progress.hidden=false;progress.value=35}const button=form.querySelector('button');if(button){button.disabled=true;button.textContent='Enviando...'}})});
