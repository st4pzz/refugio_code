(function(){document.querySelectorAll('[data-review-form]').forEach((form)=>{const comment=form.querySelector('[data-review-comment]');const counter=form.querySelector('[data-comment-count]');const updateCount=()=>{if(counter&&comment)counter.textContent=`${comment.value.length.toLocaleString('pt-BR')} / 2.000`};comment?.addEventListener('input',updateCount);updateCount();form.addEventListener('submit',(event)=>{if(!form.checkValidity()){event.preventDefault();form.reportValidity();return}const button=form.querySelector('button[type="submit"]');if(button){button.disabled=true;button.classList.add('is-loading')}})})})();
